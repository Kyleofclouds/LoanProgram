@model IEnumerable<LoanProgram.Models.ConsultantListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@if (TempData.ContainsKey("SaveResult"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveResult"]
    </div>
}
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th>
            <center>@Html.DisplayNameFor(model => model.Specialization)</center>
        </th>
        <th>
            <center>@Html.DisplayNameFor(model => model.IsCurrentEmployee)</center>
        </th>
        <th>
            <center>@Html.DisplayNameFor(model => model.HireDate)</center>
        </th>
        <th>
            <center>@Html.DisplayNameFor(model => model.TimeWith)</center>
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    @*<td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>*@
    <td>
        @Html.DisplayFor(modelItem => item.FullName)
    </td>
    <td>
        <center>@Html.DisplayFor(modelItem => item.Specialization)</center>
    </td>
    <td>
        <center>@Html.DisplayFor(modelItem => item.IsCurrentEmployee)</center>
    </td>
    <td>
        <center>@Html.DisplayFor(modelItem => item.HireDate)</center>
    </td>
    <td>
        <center>@Html.DisplayFor(modelItem => item.TimeWith)</center>
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
    }

</table>
